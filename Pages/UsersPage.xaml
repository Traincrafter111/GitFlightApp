<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:GitFlightApp.Models"
             xmlns:helpers="clr-namespace:GitFlightApp.Helpers"
             x:Class="GitFlightApp.Pages.UsersPage"
             Title="Users">

    <ContentPage.Resources>
        <ResourceDictionary>
            <helpers:ByteArrayToImageSourceHelper x:Key="BytesToImage" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="10">
            <Label Text="Users" HorizontalOptions="Center" FontAttributes="Bold" />

            <CollectionView ItemsSource="{Binding Users}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8" Margin="5">
                            <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                                <Frame WidthRequest="56" HeightRequest="56" CornerRadius="28" Padding="0" HasShadow="False" BackgroundColor="#eee">
                                    <Image Source="{Binding profileImage, Converter={StaticResource BytesToImage}}" Aspect="AspectFill" />
                                </Frame>
                                <VerticalStackLayout>
                                    <Label Text="{Binding Username}" FontAttributes="Bold" />
                                    <Label Text="{Binding UserID}" FontSize="12" TextColor="Gray" />
                                </VerticalStackLayout>
                            </HorizontalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="Agregar usuario" Clicked="OnAddNewUserClicked" HorizontalOptions="Center" Margin="0,10,0,0" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>